// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDsPJ_S7Bl6OXO3DPfLA4uPQKqBnBTIEH4",
    authDomain: "majikiti-collection.firebaseapp.com",
    databaseURL: "https://majikiti-collection.firebaseio.com",
    projectId: "majikiti-collection",
    storageBucket: "majikiti-collection.appspot.com",
    messagingSenderId: "795817683617"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
